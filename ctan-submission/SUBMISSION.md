# CTAN submission metadata - coop-writing 1.8

This file is **not** part of the upload archive. It records the fields to use when updating the existing CTAN package.

## Package

- **Name:** `coop-writing`
- **Version:** `1.8`
- **Release date:** `2026-09-23`
- **Maintainer:** Geraldo Xexéo
- **License:** MIT
- **Suggested CTAN directory:** `/macros/latex/contrib/coop-writing`
- **Repository:** https://github.com/xexeo/coop-writing
- **Bug tracker:** https://github.com/xexeo/coop-writing/issues
- **Development:** https://github.com/xexeo/coop-writing
- **Home:** https://ctan.org/pkg/coop-writing

Use the maintainer email already associated with the existing CTAN package/update account when submitting the update.

## Summary

Editorial workflow support for collaborative LaTeX documents.

## Description

`coop-writing` provides a source-controlled editorial layer for collaborative LaTeX documents. It supports comments associated with collaborators, proposed insertions/removals/replacements, TODOs, draft material, anonymization for blind submission, paragraph subject/main-idea markers, editorial lists, and mode-dependent input/include behavior.

The package is designed so that editorial information remains in the LaTeX source instead of depending on a particular editing interface such as Overleaf. The same source can therefore be compiled for editing, blind submission, or clean publication using different package modes.

Version 1.8 consolidates the complete post-1.2.4 development line currently published on GitHub. It includes the later editorial workflow features from the 1.3-1.5 series, fixes TODO default behavior and hyperref option/conditional handling, removes an unnecessary xstring dependency, and introduces reproducible CTAN packaging. The release documentation is UTF-8 and includes English and Brazilian Portuguese quick references and maximal examples.

## Announcement text

Version 1.8 is a substantial update to `coop-writing` since the CTAN 1.2.4 release. It consolidates the editorial workflow developed through the 1.3-1.5 series, including blind-review helpers, mode-dependent file inclusion, configurable anonymization display, improved suggestion handling, color helpers, and inline TODO support. It also makes inline TODO boxes explicitly opt-in, corrects hyperref conditional/option handling, removes an unnecessary xstring dependency, adds UTF-8 bilingual quick references and maximal examples, tests the documentation with pdfLaTeX and LuaLaTeX, and introduces a reproducible CTAN packaging workflow.

## Administrative note to CTAN maintainers

This is an update of the existing `coop-writing` package. The upload intentionally contains `coop-writing.dtx` and `coop-writing.ins` but not the generated `coop-writing.sty`, following CTAN guidance not to include straightforward derived files when their documented source is present. PDF documentation is included.

The archive is generated by `tools/build-ctan.sh` and has a single top-level directory named `coop-writing/`.

## Upload file

`ctan-submission/coop-writing-1.8.zip`
